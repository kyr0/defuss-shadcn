---
title: "Context Menu"
description: "Context Menu component — right-click menus with keyboard navigation."
slug: "context-menu"
---

import { DocPage, PageHeader, SkillPanel, Example, ExampleLabel, ExampleHint, ExampleCode, SourceFiles, SourceNote, StatesSection, CodeBlock, Demo, DemoCode, ChangelogEntries, ArchBody, StatsClaim, StatsCards, PageOverlay, DocText, DocLink, H2, SkillField, SkillText, SkillRow, SkillGrid, Variant, ApiPill, Dim, CodeCard, TableCard } from "../lib/components/index";

<DocPage meta={meta}>

<PageHeader slug="context-menu" title="Context Menu" component="context-menu">
  A menu triggered by right-click. Uses the Popover API with contextmenu event.
</PageHeader>

<SkillPanel component="context-menu">
  <SkillField label="Native basis">
<SkillText>Popover API triggered by right-click.</SkillText>
  </SkillField>

  <SkillField label="Web Platform APIs">
<SkillRow>
    <ApiPill href="https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/popover" title="native popover"><code>popover attribute</code></ApiPill>
    <ApiPill href="https://developer.mozilla.org/en-US/docs/Web/API/Element/contextmenu_event" title="right-click trigger"><code>contextmenu event</code></ApiPill>
  </SkillRow>
  </SkillField>

  <SkillField label="Classes">
<SkillRow>
    <code>.context-menu-trigger</code>
    <code>.context-menu</code>
    <code>.context-menu-item</code>
    <code>.context-menu-separator</code>
  </SkillRow>
  </SkillField>

  <SkillField label="Data attributes">
<SkillText>• <code>data-context-menu</code></SkillText>
  </SkillField>

  <SkillField label="State attributes (managed by JS)">
<SkillText>• <code>data-highlighted</code></SkillText>
  </SkillField>
</SkillPanel>

<Example first>
  <ExampleLabel>Default</ExampleLabel>
  <div class="context-menu-trigger" data-context-menu="demo-ctx">Right click here</div>
  <div class="context-menu" id="demo-ctx" popover data-state-demo>
    <button class="context-menu-item" role="menuitem">Cut</button>
    <button class="context-menu-item" role="menuitem">Copy</button>
    <button class="context-menu-item" role="menuitem">Paste</button>
    <div class="context-menu-separator" />
    <button class="context-menu-item" role="menuitem">Select All</button>
  </div>
  <ExampleCode>{"<div class=\"context-menu-trigger\" data-context-menu=\"my-ctx\">\n  Right click here\n</div>\n<div class=\"context-menu\" id=\"my-ctx\" popover>\n  <button class=\"context-menu-item\" role=\"menuitem\">Cut</button>\n  <button class=\"context-menu-item\" role=\"menuitem\">Copy</button>\n</div>"}</ExampleCode>
</Example>

<StatesSection>
  <p class="text-sm text-muted-foreground" style="margin:0 0 0.75rem;">Named states via the shared State API, driven per instance through the bound <code>api</code>:</p>
  <ul class="text-sm text-muted-foreground" style="margin:0 0 0.75rem;padding-left:1.25rem;">
    <li><code>default</code> — closed (the authored state; right-click opens it)</li>
    <li><code>open</code> — menu shown; <code>setState('open', \{ x, y \})</code> positions it (viewport top-left by default, there is no pointer to anchor to)</li>
  </ul>
  <p class="text-sm text-muted-foreground">The demo element carries <code>data-state-demo</code>; <code>bun run screenshots</code> drives it via <code>el.api.setState(name)</code> and captures <code>screenshots/\{mode\}/context-menu-\{state\}.png</code>.</p>
</StatesSection>

<SourceFiles component="context-menu" section="css">
  <SourceNote for="css">Styles for the context-menu component. Uses design tokens for colors, spacing, and radius.</SourceNote>
</SourceFiles>

<SourceFiles component="context-menu" section="js">
  <SourceNote for="js">Interaction logic for the context-menu component. Uses data attributes for wiring.</SourceNote>
</SourceFiles>

</DocPage>
